---
import { externalLinks } from '../data/links';
---
<footer class="container footer">
  <div class="brand-wrap"><span class="brand-mark" aria-hidden="true"></span><span>The Workbench</span></div>
  <div class="links">
    <a href={externalLinks.github} target="_blank" rel="noreferrer">GitHub</a>
    <a href={externalLinks.linkedin} target="_blank" rel="noreferrer">LinkedIn</a>
    <a href={externalLinks.readyroll} target="_blank" rel="noreferrer">ReadyRoll</a>
  </div>
</footer>
<style>
.footer{border-top:1px solid var(--color-border);padding:1.25rem 1.6rem 2.35rem;display:flex;justify-content:space-between;gap:1rem;flex-wrap:wrap;color:var(--color-muted);align-items:center}
.brand-wrap{display:inline-flex;align-items:baseline;gap:.72rem;color:var(--color-ink);font-weight:620;line-height:1}
.brand-mark{width:5px;height:20px;background:var(--color-accent);border-radius:1px;transform:translateY(2px)}
.links{display:flex;gap:1rem;flex-wrap:wrap}
.links a{text-decoration:none;color:var(--color-muted);font-size:.95rem}
.links a:hover,.links a:focus-visible{text-decoration:underline;color:var(--color-ink)}
.links a:focus-visible{outline:1px solid var(--color-accent-40);outline-offset:3px;border-radius:2px}
</style>
